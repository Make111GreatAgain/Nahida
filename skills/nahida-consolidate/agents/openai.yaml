interface:
  display_name: "Nahida Consolidate"
  short_description: "判断并整合 Nahida 现有记忆"
  default_prompt: "Use $nahida-consolidate to judge whether existing Nahida notes need upper-layer consolidation without rereading raw sources."
policy:
  allow_implicit_invocation: true
